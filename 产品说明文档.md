# 老年人健康居家管理微信小程序

一个面向**老年用户及其子女**的健康居家管理微信小程序：老人端负责日常记录与呼救，
子女端可绑定后远程只读查看父母健康数据。

> 技术形态：微信小程序原生开发（WXML/WXSS/JS/JSON）＋ 微信云开发
> （云函数 + 云数据库 + 定时触发器 + 云存储），无自建服务器。

---

## 读者导航

- 想**直接使用**成品：看“老人端使用指南”和“子女端使用指南”；
- 拿到源码想**自己部署**：看“从零配置并运行”；
- 想了解产品设计：看“功能总览”和“适老化设计”。

## 一、项目简介

中国正在进入深度老龄化社会，很多老人与子女分开居住。本项目用最简单的大字体、
大按钮交互，帮助老人完成四件事：

1. 每天测量并记录血压/血糖；
2. 按时吃药并接收服药提醒；
3. 记录饮食、活动、就医信息；
4. 遇到危险时一键呼救，并让子女远程看到自己的健康情况。

## 二、功能总览

### 老人端

| 模块 | 功能 |
| --- | --- |
| 首页 | 今日日期、本地天气与出行建议、今日服药/测血压状态 |
| 紧急呼救 | 一键拨打预设紧急联系人电话（二次确认防误触） |
| 跌倒呼救 | 红色大按钮，确认后记录位置并通知已绑定子女 |
| 健康记录 | 血压/血糖记录、历史列表、近 7 次 Canvas 趋势图 |
| 用药提醒 | 今日已服统计、添加用药、已服/未服/漏服切换 |
| 我的/档案 | 头像、年龄、病史、过敏药物、紧急联系人 |
| 子女帮设置 | 生成 6 位绑定码并分享给子女 |
| 每日活动 | 微信运动步数读取或手动记录散步时长 |
| 饮食记录 | 早/午/晚/加餐拍照上传 + 备注 |
| 就医记录 | 日期/医院/科室/诊断/建议，形成时间线 |
| 数据导出 | 全部健康数据导出为 JSON 文件 |

### 子女端

- 打开老人分享的链接输入 6 位绑定码完成绑定；
- 只读查看：今日用药、血压、血糖、饮食、就医、呼救记录；
- 老人重新生成绑定码即解除绑定关系。

## 三、适老化设计

- 页面正文不小于 36rpx（约 18px），标题与关键数字不小于 44rpx；
- 可点击区域高度不小于 110rpx，间距宽松；
- 主色深蓝 `#2C5F8A`，强调色暖橙 `#F5A623`，背景米白 `#FDF8F0`；
- 重要状态用“图标 + 文字”双重提示（✓已服 / ○未服 / ⚠漏服），不依赖红绿辨色；
- 紧急呼救两步内完成，不使用多级嵌套菜单；
- 自定义底部导航提供大图标 + 大文字标签。

## 四、技术架构

```text
项目根目录
├── cloudfunctions/    云函数（服务端）
│   ├── login/         登录建档、自动创建集合
│   ├── saveBpRecord/  血压/血糖校验写入
│   ├── getWeather/    和风天气 + 缓存 + 出行建议
│   ├── medicineReminder/ 定时服药提醒（每分钟）
│   ├── emergencyAlert/   跌倒呼救通知子女
│   ├── family/        子女绑定/解除/只读汇总
│   └── wxRun/         微信运动步数（cloudID 云调用）
└── miniprogram/       小程序前端代码
    ├── pages/         页面
    ├── custom-tab-bar/ 自定义适老化底部导航
    ├── utils/          日期等公共工具
    ├── config.js       客户端模板 ID 配置
    └── envList.js      云环境 ID 配置
```

## 五、数据与安全

主要集合：`users`（档案与绑定）、`bp_records`（血压血糖）、`medicines`（用药计划）、
`medicine_logs`（服药日志）、`activities`（活动）、`meals`（饮食）、
`medical_visits`（就医）、`emergency_logs`（呼救）、`weather_cache`（天气缓存）。

每个集合使用统一安全规则：本人可读写自己的数据，已绑定子女可只读，
越权访问在数据库层被拒绝。

## 六、从零配置并运行（给拿到源码的开发者）

> 以下所有“<占位符>”都需要替换成你自己账号的值，仓库代码里不应保留
> 任何人的 AppID、云环境 ID、天气 Key 或模板 ID。

### 0. 准备账号

- 已注册的小程序账号（个人或企业主体）；
- 微信开发者工具（最新稳定版）；
- 云开发环境（开发期可使用免费云环境）；
- 和风天气账号（免费订阅即可，用于天气）；
- 已在小程序后台添加“订阅消息”模板。

### 1. 导入项目并填写基础配置

1. 用微信开发者工具导入本仓库根目录；
2. 打开 `project.config.json`，把 `appid` 改为你的小程序 AppID；
3. 打开 `miniprogram/envList.js`，把 `envId` 改为你的云环境 ID；
4. 重新编译，确认首页能打开。

### 2. 部署云函数

对 `cloudfunctions` 下 7 个目录逐个执行：
右键 → 上传并部署：云端安装依赖：

`login`、`saveBpRecord`、`getWeather`、`medicineReminder`、
`emergencyAlert`、`family`、`wxRun`

部署后，再对 `medicineReminder` 单独执行一次“上传触发器”。

`login` 云函数会在首次运行时自动创建全部数据集合。

### 3. 配置数据库安全规则

到云开发控制台 → 数据库，对 9 个集合逐个设置“自定义安全规则”：

```json
{
  "read": "doc.openid == auth.openid || (doc.familyOpenid != null && doc.familyOpenid == auth.openid)",
  "write": "doc.openid == auth.openid"
}
```

> 这是最容易漏的一步：规则不配，页面会反复报“Permission denied / 读取失败”。

### 4. 配置天气

1. 注册和风天气并新建项目，复制 API Key；
2. 在和风控制台“设置”里复制你的专属 API Host（形如
   `https://xxxx.qweatherapi.com`）；
3. 在 `getWeather` 云函数环境变量中设置：
   - `QWEATHER_KEY` = API Key
   - `QWEATHER_HOST` = 专属 Host
   - 或直接修改 `cloudfunctions/getWeather/index.js` 顶部的内置常量。

新版和风天气要求：使用专属 Host、通过请求头 `X-QW-Api-Key` 传 Key，
响应为 gzip 压缩，代码内已处理。

### 5. 配置订阅消息模板

1. 在公众平台添加两个模板（详见根目录 README）；
2. 把“服药提醒模板 ID”填入：
   - `cloudfunctions/medicineReminder/index.js` 顶部的内置常量，或
     该函数环境变量 `MEDICINE_TEMPLATE_ID`；
   - `miniprogram/config.js` 的 `MEDICINE_TEMPLATE_ID`；
3. 把“紧急提醒模板 ID”填入 `emergencyAlert` 云函数；
4. 按你的模板字段实际编号核对云函数里的 `data` 键名
   （本项目服药模板为 `time2/thing3/thing5/character_string4`，
   紧急模板为 `thing8/time4/thing3/phone_number6/thing1`，模板不同需自行修改）。

### 6. 配置隐私保护指引

公众平台 → 设置 → 用户隐私保护指引，声明：位置信息、微信运动步数、
相册/摄像头、头像昵称，并说明健康数据的用途。

### 7. 验证

按“老人端使用指南”完整走一遍：保存档案、记录血压、添加药品、记录饮食/活动/就医，
再按“子女端使用指南”验证绑定与只读查看。

## 七、老人端使用指南

### 首次使用（3 分钟完成基础设置）

1. 打开小程序，点底部“我的”；
2. 点“编辑档案”；
3. 填写姓名、年龄、病史、过敏药物、紧急联系人姓名与电话；
4. 点“保存档案”。

> 紧急联系人电话是“首页一键呼救”要拨打的号码，建议一定要填。

### 每天怎么用

**记录血压/血糖**

1. 首页点“记录血压”（或点底部“健康记录”）；
2. 选“血压”或“血糖”；
3. 填写数值，点“保存记录”；
4. 首页会自动显示“今日已测血压”。

**服药**

1. 点底部“用药”；
2. 第一次使用时点“＋ 添加用药”，填药名、剂量、时间；
3. 弹出“订阅消息”授权时点允许，这样到点会收到提醒；
4. 每次吃药后，点药品右侧状态按钮，选“确认已服”。

**记录饮食/活动/就医**

在首页“更多服务”中分别进入“每日活动 / 饮食记录 / 就医记录”，
按页面提示填写并保存。

**紧急情况**

- 需要和家人通话：首页点“紧急呼救” → 确认 → 自动拨号；
- 摔倒或行动困难：点红色“我摔倒了” → 确认 → 记录位置并通知已绑定子女，
  随后可一键拨打紧急联系人。

## 八、子女端使用指南

### 第一次绑定

1. 请老人打开“我的 → 子女帮设置”生成 6 位绑定码；
2. 老人把绑定码发给子女（也可用小程序分享功能）；
3. 子女点开分享链接，或进入“子女帮设置”相关入口；
4. 输入 6 位绑定码，点“绑定并查看”。

### 绑定后能做什么

- 查看老人今日是否按时服药；
- 查看最近血压、血糖；
- 查看最近饮食和就医记录；
- 收到老人“我摔倒了”的紧急提醒（需老人端已配置紧急联系人和订阅消息）。

### 解除绑定

子女在查看页点“解除绑定”，或老人重新生成绑定码，绑定关系即失效。

## 九、隐私与安全

- 云数据库中的 `openid` 用于身份关联，页面不展示；
- 紧急联系电话仅由老人本人/家人在健康档案中填写；
- 子女绑定采用 6 位一次性绑定码，绑定关系可随时解除；
- 位置信息仅用于“本地天气”和“跌倒呼救定位”；
- 本程序仅做记录与提醒，不提供医疗诊断或治疗建议。

## 十、Roadmap

- [x] 第一版：档案、健康记录、用药、首页概览、紧急呼救
- [x] 第二版：跌倒呼救、活动步数、饮食、就医、子女只读端
- [ ] 语音播报（微信同声传译插件 / 云 TTS）
- [ ] 智能健康小结与异常趋势提醒
- [ ] 短信网关呼救通道
- [ ] 多子女共同查看

## 十一、常见问题

**Q：天气一直显示获取失败？**
A：检查是否已配置和风天气 Key 与专属 Host，且上传过最新版 `getWeather`。

**Q：保存档案提示 Permission denied？**
A：检查 9 个集合是否都配置了“自定义安全规则”（见“从零配置并运行”第 3 步）。

**Q：添加药品后没有收到到点提醒？**
A：需要在添加药品时允许订阅消息；正式版为一次性订阅，若需每日连续提醒，
需要额外的授权策略或长期订阅模板。

**Q：子女端看不到照片？**
A：饮食图片保存在云存储。请确认云存储权限允许同环境用户读取，或后续改为
云函数签发临时访问链接。

## 十二、许可

项目供学习与二次开发使用；开源前请自行清理个人密钥并按仓库 LICENSE 说明。
